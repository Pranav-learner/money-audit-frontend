import { ContentSkeleton } from '@/shared/components/common/content-skeleton';

/** Route-transition loading UI for authenticated pages. */
export default function Loading() {
  return <ContentSkeleton />;
}
